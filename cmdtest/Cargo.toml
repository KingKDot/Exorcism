[package]
name = "cmdtest"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
detour = { version = "0.8.0", default-features = false }
serde_json = "1"

[profile.release]
lto = "thin"
codegen-units = 1
strip = "symbols"
